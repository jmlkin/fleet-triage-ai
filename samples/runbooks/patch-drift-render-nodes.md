# Remediation Runbook — Patch drift

> **DRAFT runbook** generated by Fleet Triage AI from synthetic fleet data.
> Review before operational use.

## Summary

**18** device(s) share a dominant fault of **patch drift**, concentrated in **role=render-node (18/18), location=SEA (10/18)**. This runbook remediates the cluster and closes the upstream cause.

## Scope

| Device ID | Hostname | Role | Location | Risk |
| --- | --- | --- | --- | --- |
| `FLT-0127` | render-node-atx-127 | render-node | ATX | 96 |
| `FLT-0141` | render-node-sea-141 | render-node | SEA | 96 |
| `FLT-0133` | render-node-sea-133 | render-node | SEA | 72 |
| `FLT-0121` | render-node-atx-121 | render-node | ATX | 58 |
| `FLT-0122` | render-node-atx-122 | render-node | ATX | 48 |
| `FLT-0123` | render-node-sea-123 | render-node | SEA | 48 |
| `FLT-0124` | render-node-nyc-124 | render-node | NYC | 48 |
| `FLT-0125` | render-node-atx-125 | render-node | ATX | 48 |
| `FLT-0126` | render-node-nyc-126 | render-node | NYC | 48 |
| `FLT-0128` | render-node-nyc-128 | render-node | NYC | 48 |
| `FLT-0129` | render-node-atx-129 | render-node | ATX | 48 |
| `FLT-0130` | render-node-sea-130 | render-node | SEA | 48 |
| `FLT-0132` | render-node-sea-132 | render-node | SEA | 48 |
| `FLT-0143` | render-node-sea-143 | render-node | SEA | 48 |
| `FLT-0144` | render-node-sea-144 | render-node | SEA | 48 |
| `FLT-0147` | render-node-sea-147 | render-node | SEA | 48 |
| `FLT-0148` | render-node-sea-148 | render-node | SEA | 48 |
| `FLT-0150` | render-node-sea-150 | render-node | SEA | 48 |

## Root cause

Dominant control: `patch_drift`. The strong correlation with role=render-node (18/18), location=SEA (10/18) indicates a systemic source (e.g. a shared image, a stalled patch ring, or an offline device class) rather than isolated failures.

## Remediation steps

1. Confirm the devices' current patch level against the latest approved build.
2. Check why the patch ring did not advance (reboot deferral, maintenance window, agent health).
3. Stage the missing cumulative update to a pilot device in the same role and verify no regressions.
4. Roll the update out in waves (10% -> 50% -> 100%), forcing a reboot window for always-on devices.
5. Move chronically-deferred devices to a stricter patch ring with an enforced reboot deadline.

## Verification

- Re-run `fleet-triage scan` and confirm the affected devices drop below the at-risk threshold.
- Confirm the `Patch drift` cluster no longer appears in the report.

## Rollback

- Each step above is reversible via the same MDM policy surface; revert the policy assignment and force a sync if a regression is observed on the pilot device.
