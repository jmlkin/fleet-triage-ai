# Remediation Runbook — Disk encryption disabled

> **DRAFT runbook** generated by Fleet Triage AI from synthetic fleet data.
> Review before operational use.

## Summary

**9** device(s) share a dominant fault of **disk encryption disabled**, concentrated in **role=edit-bay (9/9), location=ATX (9/9)**. This runbook remediates the cluster and closes the upstream cause.

## Scope

| Device ID | Hostname | Role | Location | Risk |
| --- | --- | --- | --- | --- |
| `FLT-0098` | edit-bay-atx-098 | edit-bay | ATX | 54 |
| `FLT-0102` | edit-bay-atx-102 | edit-bay | ATX | 54 |
| `FLT-0104` | edit-bay-atx-104 | edit-bay | ATX | 54 |
| `FLT-0091` | edit-bay-atx-091 | edit-bay | ATX | 42 |
| `FLT-0092` | edit-bay-atx-092 | edit-bay | ATX | 42 |
| `FLT-0093` | edit-bay-atx-093 | edit-bay | ATX | 42 |
| `FLT-0094` | edit-bay-atx-094 | edit-bay | ATX | 42 |
| `FLT-0095` | edit-bay-atx-095 | edit-bay | ATX | 42 |
| `FLT-0107` | edit-bay-atx-107 | edit-bay | ATX | 42 |

## Root cause

Dominant control: `encryption_off`. The strong correlation with role=edit-bay (9/9), location=ATX (9/9) indicates a systemic source (e.g. a shared image, a stalled patch ring, or an offline device class) rather than isolated failures.

## Remediation steps

1. Verify the device is MDM-enrolled and reachable before changing disk policy.
2. Confirm a recovery key escrow exists in the MDM before enabling encryption (avoid lockout).
3. Trace the source: a shared gold image with encryption disabled will reproduce this on every reimage.
4. Enable full-disk encryption via MDM policy and confirm the recovery key escrows successfully.
5. Fix the upstream gold image so future provisioning ships with encryption on by default.

## Verification

- Re-run `fleet-triage scan` and confirm the affected devices drop below the at-risk threshold.
- Confirm the `Disk encryption disabled` cluster no longer appears in the report.

## Rollback

- Each step above is reversible via the same MDM policy surface; revert the policy assignment and force a sync if a regression is observed on the pilot device.
