{
  "_comment": "Register Fleet Triage AI with Claude Desktop. Merge the 'mcpServers' block into your config at: Windows %APPDATA%/Claude/claude_desktop_config.json | macOS ~/Library/Application Support/Claude/claude_desktop_config.json. Replace the paths with your own. No API key required.",
  "mcpServers": {
    "fleet-triage": {
      "command": "fleet-triage-mcp",
      "env": {
        "FLEET_TRIAGE_DATA": "C:/path/to/fleet-triage-ai/data/fleet.json",
        "FLEET_TRIAGE_RULESET": "C:/path/to/fleet-triage-ai/config/ruleset.yaml"
      }
    }
  }
}
